# vm_debug_ci

Restricted runtime workspace for message intake, checked arithmetic, review, and training.

The language agent must not construct a numeric result. It invokes the mounted deterministic runtime and reports its audit output.
